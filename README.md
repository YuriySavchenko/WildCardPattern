# WildCardPattern

```
Simple realisation of WildCardPattern Matching.
This problem is solving by dynamic programming.

Algorithm consists of 3 stages:
1. We must create two-deminsional boolean array and fill it by 'false' values.
2. We must set first element of main diagonal as 'true'. Such as empty symbol can only matches with empty symbol.
```
